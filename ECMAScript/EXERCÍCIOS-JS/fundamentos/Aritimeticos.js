const [a, b, c, d] = [3, 5, 1, 15]
const soma = a + b + c + d
const soma1 = -soma //Operador unário negativa um valor
console.log(soma1)
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)